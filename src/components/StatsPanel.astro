---
import workspaceData from '../data/workspace-data.json';

const { stats } = workspaceData;

const statItems = [
  { label: '项目总数', value: stats.totalProjects, icon: '📦' },
  { label: '生产级系统', value: stats.productionProjects, icon: '🚀' },
  { label: 'AI Agent', value: stats.totalAgents, icon: '🤖' },
  { label: '学习笔记', value: stats.totalNotes, icon: '📝' },
  { label: '可复用 Pattern', value: stats.totalPatterns, icon: '🧩' },
  { label: '经验档案', value: stats.totalExperienceArchives, icon: '📚' },
];
---

<section class="max-w-6xl mx-auto px-4 sm:px-6 py-12">
  <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-4">
    {statItems.map(item => (
      <div class="glass rounded-2xl p-4 text-center card-hover">
        <div class="text-2xl mb-1">{item.icon}</div>
        <div class="text-2xl sm:text-3xl font-bold text-surface-900 dark:text-white">
          {item.value}
        </div>
        <div class="text-xs text-surface-500 mt-1">{item.label}</div>
      </div>
    ))}
  </div>
</section>
